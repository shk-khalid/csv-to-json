{
  "name": "csv-json-converter",
  "version": "1.0.0",
  "description": "Node.js API that parses nested CSV files into JSON, stores the data in PostgreSQL (via Supabase), and generates age distribution reports.",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "dev": "nodemon app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.52.1",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "multer": "^2.0.2",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "keywords": [
    "csv",
    "json",
    "converter",
    "api",
    "express",
    "postgresql"
  ],
  "author": "Khalid Shaikh",
  "license": "MIT"
}
